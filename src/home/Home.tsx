import { Box } from "@mui/material";
import type { FC } from "react";

export const Home: FC<{}> = () => {
  return (
    <Box
      sx={{
        display: "flex",
        width: "100%",
        height: "100%",
      }}
    >
      Users
    </Box>
  );
};
